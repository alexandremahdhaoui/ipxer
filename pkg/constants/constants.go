package constants

// ContextKey is a type for context keys.
type ContextKey string

const (
	// ServerNameContextKey is the context key for the server name.
	ServerNameContextKey ContextKey = "serverName"
)
